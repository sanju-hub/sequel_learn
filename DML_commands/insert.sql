/*
    Definition:
        the insert command in SQL is used to insert new records into a table.

    Syntax:
        INSERT INTO table_name (column1, column2, column3, ...)
        VALUES (value1, value2, value3, ...);

    Uses:
        - Insert new records into a table.
        - Add data to a table from another table.
        - Insert default values into a table.
        - Insert data into specific columns of a table.
*/

insert into Users (user_id, username, email, phone_number);